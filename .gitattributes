# Handle line endings automatically for all text files
* text=auto eol=lf

# Ensure Github Workflow YAML files use LF, not CRLF, to avoid lint errors
.github/workflows/*.yml text eol=lf
.github/workflows/*.yaml text eol=lf
