export interface ITeam2 {
    name: string;
    chars: string[];
    mows?: string[];
    warOffense?: boolean;
    warDefense?: boolean;
    raid?: boolean;
    ta?: boolean;
    // war battlefield levels
    bfs?: boolean[];
}
