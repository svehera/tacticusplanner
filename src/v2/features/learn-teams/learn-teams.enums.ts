export enum TeamStatus {
    pending = 0,
    approved = 1,
    rejected = 2,
}

export enum SlotType {
    core = 0,
    flex = 1,
    none = 2,
}

export enum TeamsGroup {
    approved = 0,
    favorite = 1,
    myTeams = 2,
    moderation = 3,
}
