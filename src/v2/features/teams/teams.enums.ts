export enum PersonalTeamType {
    none = 0,
    tournamentArena = 1,
    guildRaids = 2,
    guildWar = 3,
}

export enum TournamentArenaType {
    none = 0,
    conquest = 1,
    powerUps = 2,
    draftPowerUps = 3,
    infestedPowerUps = 4,
}

export enum GuildRaidBossType {
    none = 0,
    avatar = 1,
    ghazghkull = 2,
    hiveTyrant = 3,
    mortarion = 4,
    rogalDorn = 5,
    screamerKiller = 6,
    szarekh = 7,
    tervigon = 8,
}

export enum GuildRaidMiniBossType {
    none = 0,
    left = 1,
    right = 2,
}

export enum GuildRaidWarType {
    none = 0,
    left = 1,
    right = 2,
}
