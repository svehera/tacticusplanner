export enum GameMode {
    tournamentArena = '_ta',
    guildRaids = '_gr',
    guildWar = '_gw',
}

export enum GuildRaidBoss {
    avatar = '_avatar',
    ghazghkull = '_ghaz',
    hiveTyrant = '_tyrant',
    mortarion = '_mortarion',
    rogalDorn = '_tank',
    screamerKiller = '_screamer',
    szarekh = '_szarekh',
    tervigon = '_tervigon',
}

export enum TaMode {
    conquest = '_con',
    powerUps = '_pu',
    draftPowerUps = '_dpu',
    infestedPowerUps = '_ipu',
}

export enum GwMode {
    offense = '_off',
    defense = '_def',
}
